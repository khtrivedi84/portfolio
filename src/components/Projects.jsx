import styles from './Projects.module.css';
import { motion } from 'framer-motion';

const Projects = () => {
  return (
    <div id="projects" className={`row justify-content-center`} style={{height:"100vh"}}>
      <h2 className={`text-center mt-4 text-light`}>Projects</h2>  {/* Centered title */}
    </div>
  );
}

export default Projects;
